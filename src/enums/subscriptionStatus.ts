export enum SubscriptionStatus {
  ACTIVE = 'active',
  INCOMPLETE = 'incomplete',
  INCOMPLETE_EXPIRED = 'incomplete_expired',
  TRAILING = 'trialing',
  UNPAID = 'unpaid',
  PAUSED = 'paused',
  PAST_DUE = 'past_due',
  CANCELED = 'canceled',
}
